1:
Iterator<String> setIterator = textSetExample.iterator();
String result = "";

while (setIterator.hasNext())
{
	result = setIterator.next();
	JOptionPane.showMessageDialog(window, result);
}

2:
q1
What are three important methods in a Set?
add, clear, and contains

q2
What properties do they have?
Everything the same type, not ordered, all unique values.